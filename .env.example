# Public deployment configuration; never place secrets here.
# The build reads the environment, not this file automatically.
# SIGNAL_URL=wss://your-signaling.example/signal
# TURN_CREDENTIALS_URL=https://your-relay.example/credentials
